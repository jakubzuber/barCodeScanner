import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    root: {
        alignItems: 'center',
        justifyContent: 'center',
        padding: 40,
        marginTop: 240,
    },
    text: {
        fontSize: 40,
        height: '20%',
    },
    subText: {
        fontSize: 20,
        height: '40%',
    },
});