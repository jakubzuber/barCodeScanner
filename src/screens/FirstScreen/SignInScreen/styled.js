import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    root: {
        alignItems: 'center',
        justifyContent: 'center',
        padding: 50,
        marginTop: 30,
    },
    text: {
        fontSize: 40,
        height: '40%',
    },
    image: {
        width: '70%',
        maxWidth: 300,
        maxHeight: 200,
        marginBottom: 40,
        borderColor:'#e8e8e8',
        borderWidth: 1,
        borderRadius: 5,
    },
});